export interface Starships {
  name: string;
  model: number;
  starship_class: string;
  manufacturer: string;
  length: string;
  max_atmosphering_speed: string;
  crew: string;
  films: string;
  pilots: string[];
}
